<%= form_with(model: keyword_badge) do |form| %>
  <% if keyword_badge.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(keyword_badge.errors.count, "error") %> prohibited this keyword_badge from being saved:</h2>

      <ul>
        <% keyword_badge.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :user_id, style: "display: block" %>
    <%= form.text_field :user_id %>
  </div>

  <div>
    <%= form.label :keywords, style: "display: block" %>
    <%= form.text_area :keywords %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
